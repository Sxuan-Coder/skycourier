/**
 * 观天驿 Skywatch Courier - 主入口
 *
 * 当前为骨架阶段。编排引擎与 Runner 实现待填充。
 * 类型契约见 src/orchestrator/types.ts、src/runner/types.ts、src/manifests/types.ts。
 */

export const VERSION = '0.1.0';
export const NAME = '观天驿 Skywatch Courier';

console.log(`${NAME} v${VERSION} - 骨架就绪，引擎待实现`);
